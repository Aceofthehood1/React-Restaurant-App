{
    "buildCommand": "npm install && npm run build",
    "framework": "vite",
    "outputDirectory": "dist",
    "rewrites": [{"src": "./backend/(.*)",
    "destination": "/backend"
    }
]
}